CREATE SCHEMA IF NOT EXISTS telco;

-- =========================
-- Dimension tables
-- =========================
DROP TABLE IF EXISTS telco.dim_tracking_area CASCADE;
CREATE TABLE IF NOT EXISTS telco.dim_tracking_area (
    tracking_area_id    INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    tracking_area_code  varchar(64) NOT NULL UNIQUE,
    description         text,
    region              varchar(64)
);

INSERT INTO telco.dim_tracking_area (tracking_area_code, description, region)
VALUES
  ('TA_1001', 'Downtown business district', 'Region_A'),
  ('TA_1002', 'Residential west zone',     'Region_A'),
  ('TA_2001', 'Airport & logistics hub',   'Region_B'),
  ('TA_2002', 'University campus area',    'Region_B');

DROP TABLE IF EXISTS telco.dim_serving_gateway CASCADE;
CREATE TABLE IF NOT EXISTS telco.dim_serving_gateway (
    serving_gateway_id  INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    sgw_name            varchar(128) NOT NULL,
    sgw_ip              inet,
    vendor              varchar(64),
    region              varchar(64)
);

INSERT INTO telco.dim_serving_gateway (sgw_name, sgw_ip, vendor, region)
VALUES
  ('SGW_01', '10.0.1.10', 'VendorX', 'Region_A'),
  ('SGW_02', '10.0.1.11', 'VendorX', 'Region_A'),
  ('SGW_03', '10.1.1.10', 'VendorY', 'Region_B'),
  ('SGW_04', '10.1.1.11', 'VendorY', 'Region_B');

DROP TABLE IF EXISTS telco.dim_serving_switch CASCADE;
CREATE TABLE IF NOT EXISTS telco.dim_serving_switch (
    serving_switch_id   INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    switch_name         varchar(128) NOT NULL,
    switch_ip           inet,
    region              varchar(64)
);

INSERT INTO telco.dim_serving_switch (switch_name, switch_ip, region)
VALUES
  ('MSC_01', '10.0.1.1', 'Region_A'),
  ('MSC_02', '10.1.1.1', 'Region_B');

DROP TABLE IF EXISTS telco.dim_subscriber CASCADE;
CREATE TABLE IF NOT EXISTS telco.dim_subscriber (
    imsi        varchar(15) PRIMARY KEY,  -- subscriber_info.IMSI
    mdn         varchar(32),              -- subscriber_info.MDN
    imei        varchar(16),              -- subscriber_info.IMEI
    version     text                      -- subscriber_info.Version
);

INSERT INTO telco.dim_subscriber (imsi, mdn, imei, version)
VALUES
  ('502120000000001', '60123400001', '356789012345671', '18'),
  ('502120000000002', '60123400002', '356789012345672', '18'),
  ('502120000000003', '60123400003', '356789012345673', '19'),
  ('502120000000004', '60123400004', '356789012345674', '20'),
  ('502120000000005', '60123400005', '356789012345675', '18'),
  ('502120000000006', '60123400006', '356789012345676', '19'),
  ('502120000000007', '60123400007', '356789012345677', '20'),
  ('502120000000008', '60123400008', '356789012345678', '18'),
  ('502120000000009', '60123400009', '356789012345679', '19'),
  ('502120000000010', '60123400010', '356789012345680', '20');

-- =========================
-- Performance base tables
-- =========================

-- ran_performance

DROP TABLE IF EXISTS telco.fact_ran_performance CASCADE;
CREATE TABLE IF NOT EXISTS telco.fact_ran_performance (
    ran_perf_id               INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    window_start_ts           timestamptz NOT NULL,
    window_end_ts             timestamptz NOT NULL,
    tracking_area_id          int NOT NULL
        REFERENCES telco.dim_tracking_area(tracking_area_id),

    ran_attach_success_rate   numeric(5,2),  -- RAN_Attach_Success_Rate
    ran_call_success_rate     numeric(5,2),  -- RAN_Call_Success_Rate
    ran_handover_success_rate numeric(5,2)   -- RAN_Handover_Success_Rate
);

CREATE INDEX IF NOT EXISTS idx_ran_perf_tracking_time
    ON telco.fact_ran_performance (tracking_area_id, window_start_ts, window_end_ts);

-- core_performance
DROP TABLE IF EXISTS telco.fact_core_performance CASCADE;
CREATE TABLE IF NOT EXISTS telco.fact_core_performance (
    core_perf_id               INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    window_start_ts            timestamptz NOT NULL,
    window_end_ts              timestamptz NOT NULL,
    tracking_area_id           int NOT NULL
        REFERENCES telco.dim_tracking_area(tracking_area_id),

    core_attach_success_rate   numeric(5,2),  -- Core_Attach_Success_Rate
    core_call_success_rate     numeric(5,2),  -- Core_Call_Success_Rate
    core_handover_success_rate numeric(5,2)   -- Core_Handover_Success_Rate
);

CREATE INDEX IF NOT EXISTS idx_core_perf_tracking_time
    ON telco.fact_core_performance (tracking_area_id, window_start_ts, window_end_ts);

-- underlay_performance

DROP TABLE IF EXISTS telco.fact_underlay_performance CASCADE;
CREATE TABLE IF NOT EXISTS telco.fact_underlay_performance (
    underlay_perf_id  INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    window_start_ts   timestamptz NOT NULL,
    window_end_ts     timestamptz NOT NULL,
    tracking_area_id  int NOT NULL
        REFERENCES telco.dim_tracking_area(tracking_area_id),

    packet_loss_pct   numeric(5,2),   -- Packet_Loss
    latency_ms        numeric(10,2),  -- Latency
    jitter_ms         numeric(10,2)   -- Jitter
);

CREATE INDEX IF NOT EXISTS idx_underlay_perf_tracking_time
    ON telco.fact_underlay_performance (tracking_area_id, window_start_ts, window_end_ts);

-- =========================
-- Call clear codes
-- =========================

-- call_clear_codes
DROP TABLE IF EXISTS telco.fact_call_clear_code CASCADE;
CREATE TABLE IF NOT EXISTS telco.fact_call_clear_code (
    call_id            INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    imsi               varchar(15) NOT NULL
        REFERENCES telco.dim_subscriber(imsi),
    tracking_area_id   int
        REFERENCES telco.dim_tracking_area(tracking_area_id),
    serving_gateway_id int
        REFERENCES telco.dim_serving_gateway(serving_gateway_id),
    serving_switch_id  int
        REFERENCES telco.dim_serving_switch(serving_switch_id),

    start_ts           timestamptz NOT NULL,
    end_ts             timestamptz,
    clear_code         varchar(32) NOT NULL  -- Clear_Code
);

CREATE INDEX IF NOT EXISTS idx_call_clear_imsi_time
    ON telco.fact_call_clear_code (imsi, start_ts);

CREATE INDEX IF NOT EXISTS idx_call_clear_tracking_time
    ON telco.fact_call_clear_code (tracking_area_id, start_ts);

-- =========================
-- Subscriber state
-- =========================

-- subscriber_state
DROP TABLE IF EXISTS telco.fact_subscriber_state CASCADE;
CREATE TABLE IF NOT EXISTS telco.fact_subscriber_state (
    subscriber_state_id   INTEGER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    imsi                  varchar(15) NOT NULL
        REFERENCES telco.dim_subscriber(imsi),

    snapshot_ts           timestamptz NOT NULL,
    vlr                   varchar(64),  -- VLR
    calling_state         varchar(32)   -- Calling_State
);

CREATE INDEX IF NOT EXISTS idx_sub_state_imsi_time
    ON telco.fact_subscriber_state (imsi, snapshot_ts);

-- =========================
-- Data generation function
-- =========================

CREATE OR REPLACE FUNCTION generate_telco_batch(p_window_seconds int DEFAULT 5)
RETURNS void AS $$
DECLARE
    base_ts   timestamptz := date_trunc('second', now());
    win_start timestamptz := base_ts;
    win_end   timestamptz := base_ts + (p_window_seconds || ' seconds')::interval;

    -- Tracking areas
    ta_1001   int := 1;  -- good
    ta_1002   int := 2;  -- good
    ta_2001   int := 3;  -- bad underlay, bad SGW_03 calls
    ta_2002   int := 4;  -- good

    -- Serving gateways
    sgw_01    int := 1;  -- good
    sgw_02    int := 2;  -- good
    sgw_03    int := 3;  -- bad
    sgw_04    int := 4;  -- good

    -- Serving switches (all “good” for simplicity)
    sw_1      int := 1;
    sw_2      int := 2;

    -- KPIs: RAN/Core good everywhere
    ran_attach  numeric(5,2) := random() * 0.5 + 99.5;  -- Random value between 99.5 and 100
    ran_call    numeric(5,2) := random() * 0.5 + 99.0;  -- Random value between 99.0 and 99.5
    ran_ho      numeric(5,2) := random() * 0.5 + 98.5;  -- Random value between 98.5 and 99.0

    core_attach numeric(5,2) := random() * 0.3 + 99.7;  -- Random value between 99.7 and 100.0
    core_call   numeric(5,2) := random() * 0.3 + 99.3;  -- Random value between 99.3 and 99.6
    core_ho     numeric(5,2) := random() * 0.3 + 99.0;  -- Random value between 99.0 and 99.3

    -- Underlay good vs bad
    pkt_loss_good numeric(5,2)  := random() * 0.4 + 0.1;  -- Random value between 0.1 and 0.5
    lat_good      numeric(10,2) := random() * 90 + 10;   -- Random value between 10 and 100
    jit_good      numeric(10,2) := random() * 2 + 3;      -- Random value between 3 and 5

    pkt_loss_bad  numeric(5,2)  := random() * 4.9 + 5.0; -- Random value between 5.0 and 9.9
    lat_bad       numeric(10,2) := random() * 199 + 201;   -- Random value between 201 and 400
    jit_bad       numeric(10,2) := random() * 470 + 30;     -- Random value between 30 and 500
BEGIN
    -- 1) RAN & Core performance – good in ALL tracking areas

    -- TA_1001
    INSERT INTO telco.fact_ran_performance (
        window_start_ts, window_end_ts, tracking_area_id,
        ran_attach_success_rate, ran_call_success_rate, ran_handover_success_rate
    ) VALUES (win_start, win_end, ta_1001, ran_attach, ran_call, ran_ho);

    INSERT INTO telco.fact_core_performance (
        window_start_ts, window_end_ts, tracking_area_id,
        core_attach_success_rate, core_call_success_rate, core_handover_success_rate
    ) VALUES (win_start, win_end, ta_1001, core_attach, core_call, core_ho);

    -- TA_1002
    INSERT INTO telco.fact_ran_performance (
        window_start_ts, window_end_ts, tracking_area_id,
        ran_attach_success_rate, ran_call_success_rate, ran_handover_success_rate
    ) VALUES (win_start, win_end, ta_1002, ran_attach, ran_call, ran_ho);

    INSERT INTO telco.fact_core_performance (
        window_start_ts, window_end_ts, tracking_area_id,
        core_attach_success_rate, core_call_success_rate, core_handover_success_rate
    ) VALUES (win_start, win_end, ta_1002, core_attach, core_call, core_ho);

    -- TA_2001 (bad underlay, but RAN/Core still look good)
    INSERT INTO telco.fact_ran_performance (
        window_start_ts, window_end_ts, tracking_area_id,
        ran_attach_success_rate, ran_call_success_rate, ran_handover_success_rate
    ) VALUES (win_start, win_end, ta_2001, ran_attach, ran_call, ran_ho);

    INSERT INTO telco.fact_core_performance (
        window_start_ts, window_end_ts, tracking_area_id,
        core_attach_success_rate, core_call_success_rate, core_handover_success_rate
    ) VALUES (win_start, win_end, ta_2001, core_attach, core_call, core_ho);

    -- TA_2002
    INSERT INTO telco.fact_ran_performance (
        window_start_ts, window_end_ts, tracking_area_id,
        ran_attach_success_rate, ran_call_success_rate, ran_handover_success_rate
    ) VALUES (win_start, win_end, ta_2002, ran_attach, ran_call, ran_ho);

    INSERT INTO telco.fact_core_performance (
        window_start_ts, window_end_ts, tracking_area_id,
        core_attach_success_rate, core_call_success_rate, core_handover_success_rate
    ) VALUES (win_start, win_end, ta_2002, core_attach, core_call, core_ho);

    -- 2) Underlay: TA_2001 bad, others good

    -- TA_1001
    INSERT INTO telco.fact_underlay_performance (
        window_start_ts, window_end_ts, tracking_area_id,
        packet_loss_pct, latency_ms, jitter_ms
    ) VALUES (win_start, win_end, ta_1001,
              pkt_loss_good, lat_good, jit_good);

    -- TA_1002
    INSERT INTO telco.fact_underlay_performance (
        window_start_ts, window_end_ts, tracking_area_id,
        packet_loss_pct, latency_ms, jitter_ms
    )
    VALUES (win_start, win_end, ta_1002,
            pkt_loss_good, lat_good, jit_good);

    -- TA_2001 (bad)
    INSERT INTO telco.fact_underlay_performance (
        window_start_ts, window_end_ts, tracking_area_id,
        packet_loss_pct, latency_ms, jitter_ms
    )
    VALUES (win_start, win_end, ta_2001,
            pkt_loss_bad, lat_bad, jit_bad);

    -- TA_2002
    INSERT INTO telco.fact_underlay_performance (
        window_start_ts, window_end_ts, tracking_area_id,
        packet_loss_pct, latency_ms, jitter_ms
    )
    VALUES (win_start, win_end, ta_2002,
            pkt_loss_good, lat_good, jit_good);

    -- 3) Subscriber state snapshots (optional, just to have data)

    INSERT INTO telco.fact_subscriber_state (imsi, snapshot_ts, vlr, calling_state)
    VALUES
      -- good TAs
      ('502120000000001', win_start, 'VLR_1001', 'ACTIVE'),
      ('502120000000002', win_start, 'VLR_1001', 'ACTIVE'),
      ('502120000000003', win_start, 'VLR_1002', 'ACTIVE'),
      ('502120000000004', win_start, 'VLR_1002', 'ACTIVE'),
      ('502120000000005', win_start, 'VLR_2002', 'ACTIVE'),
      ('502120000000006', win_start, 'VLR_2002', 'ACTIVE'),
      ('502120000000007', win_start, 'VLR_1001', 'ACTIVE'),
      ('502120000000008', win_start, 'VLR_1002', 'ACTIVE'),
      -- bad TA
      ('502120000000009', win_start, 'VLR_2001', 'ACTIVE'),
      ('502120000000010', win_start, 'VLR_2001', 'ACTIVE');

    -- 4) Calls
    -- IMSI 1–8: always in GOOD TAs, GOOD SGWs, NORMAL_CLEARING
    -- IMSI 9–10: in TA_2001 and SGW_03, BAD clear codes

    -- Good calls (IMSI 1–8)
    INSERT INTO telco.fact_call_clear_code (
        imsi, tracking_area_id, serving_gateway_id, serving_switch_id,
        start_ts, end_ts, clear_code
    ) VALUES
      ('502120000000001', ta_1001, sgw_01, sw_1,
       win_start, win_end,
       'NORMAL_CLEARING'),
      ('502120000000002', ta_1001, sgw_01, sw_1,
       win_start, win_end,
       'NORMAL_CLEARING'),
      ('502120000000003', ta_1002, sgw_02, sw_1,
       win_start, win_end,
       'NORMAL_CLEARING'),
      ('502120000000004', ta_1002, sgw_02, sw_1,
       win_start, win_end,
       'NORMAL_CLEARING'),
      ('502120000000005', ta_2002, sgw_04, sw_2,
       win_start, win_end,
       'NORMAL_CLEARING'),
      ('502120000000006', ta_2002, sgw_04, sw_2,
       win_start, win_end,
       'NORMAL_CLEARING'),
      ('502120000000007', ta_1001, sgw_01, sw_1,
       win_start, win_end,
       'NORMAL_CLEARING'),
      ('502120000000008', ta_1002, sgw_02, sw_1,
       win_start, win_end,
       'NORMAL_CLEARING');

    -- Bad calls (IMSI 9–10) – TA_2001, SGW_03
    INSERT INTO telco.fact_call_clear_code (
        imsi, tracking_area_id, serving_gateway_id, serving_switch_id,
        start_ts, end_ts, clear_code
    ) VALUES
      ('502120000000009', ta_2001, sgw_03, sw_2,
       win_start, win_end,
       'SGW_CONGESTION_FAILURE'),
      ('502120000000010', ta_2001, sgw_03, sw_2,
       win_start, win_end,
       'SGW_INTERNAL_ERROR');
END;
$$ LANGUAGE plpgsql;